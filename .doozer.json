{
  "submodules": ["simulation/libsimulator"],
  "targets": {
    "macOS": {
      "buildenv": "osx",
      "homebrew": { "formulae": ["boost", "boost-build", "openssl"] },
      "buildcmd": ["ls -la /usr/local/include", "cd test", "b2 -j${PARALLEL} crypto=openssl"]
    }
  }
}

